/**
 * Entry point for the pharmacy-ufe component library.
 * Re-exports trivial helpers + the generated component type declarations.
 */

export { format } from './utils/utils';
export type * from './components.d.ts';
